Installation procedure for Duro
===============================

Duro uses automake and autoconf to build itself. Type:

./configure
make
make check    (optional)
make install

To compile Duro, you need GNU Libtool and Berkeley DB.

Berkeley DB is assumed to be located in /usr/local/BerkeleyDB4.1.
If Berkeley DB is located in another directory, set the BDB_HOME
environment variable and use the -e option of make.
